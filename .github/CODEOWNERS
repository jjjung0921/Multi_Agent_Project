# 생성 파일 — docs/ARCHITECTURE.md Module Boundaries 의 Owner 열에서 `scripts/ai-stream.sh codeowners` 가 만든다. 손으로 고치지 않는다 (ai-end.sh --ci 가 drift 를 잡는다).
# 초기화 전 예시:
# docs/            @lead
# src/backend/     @backend-owner
# .ai/team/        @lead
